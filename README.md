# ext26

## Browser extension for Firefox

This extension is designed to dynamically modify the content layout of the website **secret4u.ru**.
The site has poor markup and layout issues on mobile devices.

If you want to use the extension on other websites, you need to change the domain in the `manifest.json` file.